name: CiderCMS

Plugin::Authentication:
    default:
        credential:
            class: Password
            password_type: hashed
            password_hash_type: SHA-256
        store:
            class: CiderCMS
